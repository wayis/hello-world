{
  "name": "hello-world",
  "version": "0.1.0",
  "scripts": {
    "test": "mocha --recursive --timeout 5000"
  },
  "dependencies": {},
  "engines": {
    "node": ">=0.10.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.4.5"
  }
}
